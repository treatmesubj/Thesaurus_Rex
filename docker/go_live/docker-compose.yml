version: "3"
services:
  nginx_reverse_proxy:
    build: ./services/nginx
    hostname: nginx
    volumes:
      - ../letsencrypt:/letsencrypt
      - ../letsencrypt/certs:/etc/letsencrypt
    ports:
      - 443:443
      - 80:80
  waitress_flask_wsgi:
    build: ./services/waitress_flask
    hostname: flask
    expose:
      - 8000
  certbot:
    build: ./services/certbot
    hostname: certbot
    expose:
      - 9999  # for kompose
    volumes:
      - ../letsencrypt:/letsencrypt
      - ../letsencrypt/certs:/etc/letsencrypt
    stdin_open: true
    tty: true
